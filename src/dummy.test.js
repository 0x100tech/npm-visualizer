const assert = require('assert');
const index = require('./index');

/* eslint-disable no-undef */
describe('dummy', () => {
  it('should return true', () => {
    assert.equal(true, index.dummy());
  });
});
